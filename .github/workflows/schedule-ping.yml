name: schedule ping

on:
  workflow_dispatch:
  schedule:
    # Minimal schedule probe: UTC 08:30 = SGT 16:30
    - cron: '30 8 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Prove schedule fired
        run: |
          echo "schedule ping ok"
          date -u
